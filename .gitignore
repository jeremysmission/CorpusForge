# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Data (large files - do not commit)
data/source/
data/output/
*.sqlite3
*.faiss
*.f16.dat
*.npy
*.npz
*.lance/

# Sanitization script — NEVER push to remote (contains replacement patterns)
sanitize_before_push.py

# Machine-specific config overrides (workers, GPU, paths)
config/config.local.yaml

# CoPilot+ instruction file — never push to remote
CLAUDE.md

# Production source data — never push to remote
ProductionSource/

# Credentials / secrets
.env
*.key
*.pem
credentials.json

# Logs
logs/
*.log

# Test output
.pytest_cache/
htmlcov/
.coverage
